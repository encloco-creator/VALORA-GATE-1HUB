---
import FadeUp from "../components/animations/FadeUp.jsx";
---

<section class="relative flex flex-col items-center justify-center h-screen text-center overflow-hidden bg-[#0B0B0C]">
  <!-- Vídeo de fundo -->
  <video
    autoplay
    muted
    loop
    playsinline
    class="absolute inset-0 w-full h-full object-cover opacity-30"
  >
    <source src="/video/hero.mp4" type="video/mp4" />
  </video>

  <!-- Conteúdo -->
  <div class="relative z-10 max-w-3xl px-6">
    <FadeUp client:load>
      <h1 class="text-5xl md:text-6xl font-semibold mb-6 gradient-text leading-tight">
        A porta de entrada para o futuro do comércio global
      </h1>
    </FadeUp>

    <FadeUp client:load delay={0.2}>
      <p class="text-lg text-gray-300 mb-8">
        A <span class="text-white font-semibold">Valora</span> integra empresas globais em um ecossistema de automação e dados —
        simplificando logística, câmbio e compliance com inteligência artificial.
      </p>
    </FadeUp>

    <FadeUp client:load delay={0.4}>
      <a
        href="/company"
        class="inline-block px-8 py-3 rounded-full border border-white/20 bg-white/10 hover:bg-white/20 transition backdrop-blur-md"
      >
        Saiba mais
      </a>
    </FadeUp>
  </div>
</section>
